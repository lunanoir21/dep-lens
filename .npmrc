# Development installs skip the platform binary packages: they are produced
# by the release workflow and are not available from the registry while
# working in this repository. The CLI falls back to target/release locally.
omit=optional
