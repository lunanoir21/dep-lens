[project]
name = "python-test-project"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31,<3",
    "flask>=3.0",
    "click",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
